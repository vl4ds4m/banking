services:
  timeout: 5

  currency-rates:
    url: ${RATES_URL:http://rates:8080/rates}

  keycloak:
    url: ${KEYCLOAK_URL}
    realm: ${KEYCLOAK_REALM}

client:
  id: ${CLIENT_ID}
  secret: ${CLIENT_SECRET}
