apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "randint-chart.fullname" . }}-test-connection"
  labels:
    {{- include "randint-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "randint-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
