apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "liquibase-accounts.fullname" . }}-test-connection"
  labels:
    {{- include "liquibase-accounts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "liquibase-accounts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
