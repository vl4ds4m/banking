server {
    listen 80;

    location / {
        proxy_set_header Host $host;
        proxy_pass http://webui:8080/;
    }

    location /auth/ {
        proxy_set_header Host $host;
        proxy_pass http://keycloak:8080/;
    }
}
